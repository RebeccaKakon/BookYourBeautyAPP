<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookyourbeauty">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookYourBeauty">
        <activity android:name=".ManegerOptionActivity"></activity>
        <activity android:name=".ClientProfileActivity" />
        <activity android:name=".CancelAppointmentActivity" />
        <activity android:name=".BookAppointmentActivity" />
        <activity android:name=".ClientOptionActivity" />
        <activity android:name=".EnterManegerActivity" />
        <activity android:name=".EnterActivity" />
        <activity android:name=".RegisterWorking" />
        <activity android:name=".Manager" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>